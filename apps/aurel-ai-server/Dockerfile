# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build aurel-ai-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t aurel-ai-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system aurel-ai-server && \
          adduser --system -G aurel-ai-server aurel-ai-server

COPY  aurel-ai-server aurel-ai-server
RUN chown -R aurel-ai-server:aurel-ai-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix aurel-ai-server --omit=dev -f install

CMD [ "node", "aurel-ai-server" ]
